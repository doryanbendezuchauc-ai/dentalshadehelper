# GITHUB_TOKEN: Required for GitHub Models inference API calls.
# Use a GitHub Personal Access Token (fine-grained PAT) with appropriate permissions.
GITHUB_TOKEN="YOUR_GITHUB_PAT"

# APP_URL: The URL where this applet is hosted.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
